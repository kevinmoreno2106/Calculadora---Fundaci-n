pulsar botones
    escribimos un valor

pulsar operacion
    guardamos el valor e iniamos un nuevo valor
    ejecuta y el el nuevo valor se convierte en el valor inicial, se guarda e iniciamos un nuevo valor


tecla borrar
    elimina la ultima pulsacion del valor en pantalla

limpiar (C)
    vuelve a sus valores iniciales

ejecutar (=) 
    ejecuta tomando el valor 1, el operador  y el segundo valor




Inputs 


pantalla
    obtenemos los valores y mostramos los valores

    valor 1 y valor 2, nuestras variables que almacenan los valores
    operador, almacena el operador y ejecuta codigo




si existen 2 valores, ejecuto la operacion, si existe un solo valor, cambio el operador

